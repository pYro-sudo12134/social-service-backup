databaseChangeLog:
  - changeSet:
      id: 000-1
      author: losik
      runOrder: first
      runAlways: true
      changes:
        - sql:
            sql: CREATE SCHEMA IF NOT EXISTS user_schema
            comment: Создание схемы user_schema для таблицы users